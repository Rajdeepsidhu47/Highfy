#include <stdio.h>

int main(void) {
	// your code goes here
	int T;
	scanf("%d", &T);
	for(int i=0; i<T; i++)
	{
	    int X,Y;
	    scanf("%d%d", &X,&Y);
	    if(X>10*Y)
	    {
	        printf("YES\n");
	    }
	    else
	    {
	        printf("NO\n");
	    }
	}
	return 0;
}

