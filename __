#include <stdio.h>

int main(void) {
	// your code goes here
	int t;
	scanf("%d", &t);
	for(int i=0; i<t; i++);
	{
	    int x=0,y=0;
	    scanf("%d%d", &x, &y);
	    printf("%d\n", x*y);
	}
	return 0;
}
